<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>update blog</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #878484;
            margin: 0;
            padding: 0;
            
        }

        h1 {
            text-align: center;
            margin-top: 20px;
        }

        form {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #343a40;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
        }

        label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="url"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        textarea {
            resize: vertical;
            height: 100px;
        }

        input[type="submit"] {
            background-color: #007BFF;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 4px;
            font-size: 18px;
        }

        input[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>update blog</h1>
    <form action="http://localhost:1000/final-update" method="POST">


        <input type="hidden" value="<%=result.id%>" name="id" />
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= result.title %>" required>

        <label for="photoLink">Photo Link:</label>
        <input type="text" id="photoLink" name="img_url" value="<%= result.img_url%>" required>

        <label for="description">Description:</label>
        <input type="text" id="des" name="des" value="<%= result.des %>" required>

        <input type="submit" value="update">
    </form>
</body>
</html>
